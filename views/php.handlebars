  <a href="signout">Sign Out</a>
</div>
</div>
<div class="col-container servlet">
  <div class="col-6">
  	<div class="picture">
  		<img class = "banner" src="static/images/php.jpg">
  	</div>
  </div>
</div>

<div class="body php">
  <h2>PHP: Hypertext Preprocessor</h2>
  <div class="java-def php">
    <p>Php or Hypertext Preprocessor is a server  scripting language and considered as a powerful tool when it comes to making interactive web pages. It is a free and open source scripting language. It allows the users to interact with database.</p>
    <h3>What is a PHP File?</h3>
    <p>PHP File contains:</p>
    <ul>
      <li>HTML</li>
      <li>CSS</li>
      <li>JavaScript</li>
      <li>PHP Code</li>
      <li>Text</li>
    </ul>
    <h3>What does PHP do?</h3>
    <ul>
      <li>PHP can encrypt data.</li>
      <li>In PHP, you can add, delete and even modify elements within your database. </li>
      <li>PHP performs system functions</li>
      <li>PHP can send and receive cookies</li>
      <li>It restrict user to access some pages in the website.</li>
    </ul>
    <h3>PHP Syntax</h3>
    <ul>
      <li>PHP functions are not case sensitive</li>
      <li>PHP statements end with a semicolon (;)</li>
      <li>PHP starts with <?php and it end with ?>. For example: <? php //insert code here ?></li>
    </ul>
    <h3>Comments in PHP</h3>
    <p>PHP has several ways on how to put a comment in a php code.</p>
    <ul>
      <li>1. Using <b>//</b> - <b>//</b>this is a single line comment</li>
      <li>2. Using <b>#</b> - <b>#</b>this is a single line comment</li>
      <li>3. Using <b>/**/</b> -<b>/*</b>This is a multiple line comment<b>*/</b></li>
    </ul>
    <h3>Rules for PHP Variables:</h3>
    <ul>
      <li>Variable must start with a $ sign, then followed by the variable name.</li>
      <li>Variable name cannot start with a number</li>
      <li>Variable names are case sensitive.</li>
    </ul>
    <h3>PHP Keywords</h3>
    <ul>
      <li><b>global</b> - It is used to access a global variable within a function.</li>
      <li><b>static</b> - each time the function called, the variable will still have the information it has from the last time the function was called.</li>
    </ul>
    <h3>Echo and print Statements</h3>
    <p>The <b>echo</b> and the <b>print</b> are somehow the same. They are both used to display the output to the screen. The <b>echo</b> has no return value while the <b>print</b> has a return value of 1 which can be used in expressions. <b>echo</b> can have multiple parameters while <b>print</b> can only have one argument. Overall, <b>echo</b> is way faster than <b>print</b>.<p>
    <h3> PHP Data Types</h3>
      <p> <b>PHP</b> supports the following data types:
        <li><b>String</b> - sequence of characters.</li>
        <li><b>Integer</b> - non decimal number.</li>
        <li><b>Float</b> - number with decimal point. </li>
        <li><b>Boolean</b> - repserents two possible states: <b>True</b> or <b>False</b></li>
        <li><b>Array</b> - stores multiple value in one variable.</li>
        <li><b>Object</b> -stores data and information on how to process data. </li>
        <li><b>Resource</b> - storing of a reference to functions.</li>
        <li><b>Null</b> - variable that has no value assigned to it.</li>
  </div>
</div>

<button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#mymodal">open quiz</button>
<div class="modal fade" id="mymodal" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <button type="button" class="close" data-dismiss="modal">&times;</button>
      <form method="POST" action="getAnswers">
        {{#each questionArr }}
          <p>{{showid}}. {{content}}</p>
          {{#each options }}
            <input type="radio" name={{../name}} value={{code}} />{{code}}. {{option}}<br>
          {{/each}}
          <br>
        {{/each }}
        <hr>
        <button class="send">Send</button>
      </form>
      <button type="button" class="btn btn-default" data-dismiss="modal">close</button>
    </div>
  </div>
</div>
{{#if remaining}}
  <script>
    alert("you need to answer " + {{remaining}} + " questions");
  </script>
{{/if}}
